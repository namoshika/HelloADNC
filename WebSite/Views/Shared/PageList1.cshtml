@model ListVM<Page>
@{
    ViewData["TITLE_PAGE"] = Model.Current.Title;
}

<div class="row">
    <div class="col-md-12 col-section">
        <div id="contentsGuide" class="section">
            <h2 class="title">@Model.Current.Title</h2>
            <p class="text">@Model.Current.Summary</p>
        </div>
    </div>
</div>
<div class="row row-groupBox">
    @foreach (var childPage in Model.Items)
    {
        <div class="col-md-4 col-sm-6 col-section">
            <div class="section section-catalog">
                <img src="@Url.Content(childPage.ThumbnailUrl)" class="picture" width="250" height="100" alt="thumb" />
                <h4 class="title">@childPage.Title</h4>
                <span class="text">
                    @childPage.Summary
                    <a asp-controller="@childPage.Controller"
                       asp-action="@childPage.Action"
                       asp-all-route-data="childPage.GetParameters()">[開く]</a>
                </span>
            </div>
        </div>
    }
</div>
